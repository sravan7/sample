import { createMuiTheme } from '@material-ui/core/styles'
const theme = createMuiTheme({
  palette: {
    text: {
      primary: '#000000',
      secondary: '#777777'
    },
    background: {
      default: '5F99F4'
    }
  },
})

export default theme